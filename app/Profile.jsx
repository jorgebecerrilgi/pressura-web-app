"use client";

const Profile = () => {
    return (
        <button onClick={() => console.log("Profile opened.")}>
            <img src="/icon-profile.svg" height="32px"></img>
        </button>
    );
};

export default Profile;
